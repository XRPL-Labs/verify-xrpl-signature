{
  "name": "verify-xrpl-signature",
  "version": "3.0.0",
  "description": "Verify XRPL signed TX blob",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublish": "npm run clean && npm run build && npm run test",
    "clean": "rm -rf dist",
    "build": "tsc",
    "watch": "tsc -w",
    "dev": "npm run build; nodemon dist/index.js",
    "pull": "git pull && npm run pm2 && pm2 monit",
    "test": "npm run clean && npm run build && jest --ci --verbose"
  },
  "files": [
    "dist/**/*.js",
    "dist/**/*.js.map",
    "dist/**/*.d.ts"
  ],
  "directories": {
    "test": "test"
  },
  "overrides": {
    "ripple-binary-codec": "npm:xrpl-binary-codec-prerelease@6.0.0"
  },
  "dependencies": {
    "ripple-keypairs": "^1.1.4",
    "xrpl-accountlib": "^3.1.0"
  },
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "@types/node": "^12.12.14",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7",
    "typescript": "4.5"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/XRPL-Labs/verify-xrpl-signature.git"
  },
  "bugs": {
    "url": "https://github.com/XRPL-Labs/verify-xrpl-signature/issues"
  },
  "homepage": "https://github.com/XRPL-Labs/verify-xrpl-signature#readme",
  "license": "MIT",
  "readmeFilename": "README.md",
  "keywords": [
    "xrp",
    "xrpl",
    "signature",
    "tx",
    "blob"
  ]
}
